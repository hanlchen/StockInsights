fastapi==0.115.0
uvicorn[standard]==0.30.6
# Unpinned (min version only): Yahoo Finance frequently changes its
# anti-bot/auth requirements and yfinance ships fixes often. Pinning an
# older version here is what caused quote fetches to fail -- always want
# the latest compatible release. curl_cffi is what recent yfinance uses to
# impersonate a real browser and get past Yahoo's crumb/cookie gating.
yfinance>=0.2.50
curl_cffi>=0.7.1
pandas==2.2.2
sqlalchemy==2.0.34
# Postgres driver -- needed once DATABASE_URL points at Supabase instead of
# SQLite (binary wheel, no separate libpq install required). Unused/harmless
# if you're still on the default sqlite:/// URL.
psycopg2-binary==2.9.9
pydantic-settings==2.4.0
pandas-datareader==0.10.0
requests==2.32.3
python-dotenv==1.0.1
# Needed for pandas.read_html in services/sp500.py -- parses the Wikipedia
# S&P 500 constituents table (no free official index-membership API exists).
lxml==5.3.0
