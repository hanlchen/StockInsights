# Shared FastAPI dependencies. Empty for the single-user MVP; this is the
# natural place to add `get_current_user` once auth ships (see architecture
# doc's SaaS scaling table) without having to touch every route file.
